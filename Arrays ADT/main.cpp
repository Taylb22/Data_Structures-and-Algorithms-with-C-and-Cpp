#include <iostream>
#include <stdlib.h>
#include "Array.h"

//clear the console
void clear() {

}

int main () {
    clear();
    int n;
    std::cout << "Welcome to the ARRAY testing program" << std::endl;

    Array<double> arr(n);

    int aux = 0;
    while (true) {
        switch (aux) {
            case 0:
                break;
            case 1:
                break;
            case 2:
                break;
        }

        if (aux == 0) {
            break;
        }
    }

    return 0;
}